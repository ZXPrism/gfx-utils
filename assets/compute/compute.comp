#version 430
layout(local_size_x = 256) in;

// layout(std430, binding = 0) buffer DataBuffer {
// float data[];
// };

uniform float factor;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    data[idx] = float(idx) * factor;
}
